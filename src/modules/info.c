#include "info.h"

//executeInfoCheck
// - Given a path, display the metadata of the file. Specifically,
//   type of file, device on which file is located, inode number, permissions, size, path to a parent directory}
int executeInfoCheck(char * filename) {

    printf("executeInfoCheck");
    return RETURN_FAILURE;
}